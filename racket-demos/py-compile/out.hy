(import express) (import tslib) (import (tslib (*))) (setv angle 90) (defn setup () (global angle)) (defn update () (global angle) (do (print angle) (do (set-servo-f board.A1 angle)))) (setup) (while True (if express.cpx.button_b (do (print "B") (setv angle 0))) (if express.cpx.button_a (do (print "A") (setv angle 180))) (ir-receive) (update))