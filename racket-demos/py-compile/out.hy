(import express) (import tslib) (import (tslib (*))) (setv speed 1) (defn setup () (global speed)) (defn update () (global speed) (global state) (do (do (enable-touch-a7 False) (pin-write-f board.A7 True)) (do (time.sleep speed) (express.cpx.pixels.show)) (do (enable-touch-a7 False) (pin-write-f board.A7 False)) (do (time.sleep speed) (express.cpx.pixels.show)))) (setup) (while True (if (not (= express.cpx.touch_A2 touch_a2_prev)) (if express.cpx.touch_A2 (do (setv speed 1)))) (if (not (= express.cpx.touch_A1 touch_a1_prev)) (if express.cpx.touch_A1 (do (setv speed 0.5)))) (update-buttons) (ir-receive) (update))